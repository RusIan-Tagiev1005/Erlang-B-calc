Erlang B Calculator (CLI)

Консольный калькулятор Эрланга B, реализованный на языке C. Программа предназначена для инженерных расчётов в телекоммуникациях: оценки вероятности потерь вызовов, определения требуемого числа каналов, нагрузки и среднего числа занятых каналов.

Проект полезен для проектирования АТС, SIP‑транков, колл‑центров и учебных работ по теории телетрафика.

📐 Используемая модель

Используется классическая формула Erlang B для систем без очереди (потерянные вызовы не ожидают обслуживания):

B(A, V) = (A^V / V!) / Σ(A^k / k!), k = 0..V

Где:

A — нагрузка (эрланги)

V — количество каналов

B(A,V) — вероятность потерь (Percentage of lost tickets)

Дополнительно используется соотношение:

m = A · (1 − E)

где m — среднее число занятых каналов.

⚙️ Возможности

Программа умеет рассчитывать:

вероятность потерь E

среднее число занятых каналов m

требуемое количество каналов v

допустимую нагрузку a

Расчёты выполняются с использованием:

бинарного поиска

метода бисекции

🛠 Требования

Компилятор C (gcc / clang)

Стандартная библиотека C

📦 Сборка

git clone https://github.com/RusIan-Tagiev1005/Erlang-B-calc.git
cd Erlang-B-calc
gcc erlangb.c -o erlangb

🚀 Использование

Программа работает через аргументы командной строки.

Общий синтаксис

./erlangb [параметр] [значение] [параметр] [значение]

Поддерживаемые параметры

Параметр

Описание

-h

справка

-a

нагрузка (эрланги), a > 0

-v

количество каналов, целое число

-m

среднее число занятых каналов

-E

вероятность потерь, 0 < E < 1

🔄 Поддерживаемые режимы расчёта

1️⃣ Нагрузка и каналы → потери и занятость

./erlangb -a 10 -v 5

Вывод:

Percentage of lost 0.563952
Average number of busy channels: 4.360478

2️⃣ Нагрузка и потери → каналы и занятость

./erlangb -a 10 -E 0.01

Вывод:

Required number of channels: 15
Average number of busy channels: 9.900000

3️⃣ Каналы и потери → нагрузка и занятость

./erlangb -v 10 -E 0.05

Вывод:

Load: 7.82
Average number of busy channels: 7.43
